#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
using ll = long long;
int main(void){
    // Your code here!
    ll A,B;
    cin>>A>>B;
    ll x=pow(32,(A-B));
    
    cout << fixed << setprecision(10);
    cout<<x;
}
