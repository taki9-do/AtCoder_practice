#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
using ll = long long;
int main(void){
    // Your code here!
    string N;cin>>N;
    
    ll ans=0;
    rep(i,N.size()) ans+=N[i]-'0';
    
    if(ans%9==0) cout<<"Yes";
    else cout<<"No";
    
}
