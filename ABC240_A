#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
int main(void){
    // Your code here!
    int a,b;cin>>a>>b;
    if(abs(a-b)==1||abs(a-b)==9) cout<<"Yes";
    else cout<<"No";
}
